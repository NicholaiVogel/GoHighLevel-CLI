# Installation

Phase 0 supports source builds only.

```bash
cargo build --workspace
cargo run -p ghl-cli -- --help
```

Release binaries, npm wrapper, and curl installer are planned for the distribution phase.

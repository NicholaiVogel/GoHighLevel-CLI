# Capabilities

Capability discovery is specified in `docs/SPEC.md` and will be implemented after auth and safe-read HTTP commands exist.

Phase 0 command metadata marks all implemented commands as local and offline-safe.
